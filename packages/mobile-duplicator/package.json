{
  "name": "mobile-duplicator",
  "displayName": "SWG Mobile Duplicator",
  "description": "Duplicate mobile creatures with complete appearance chains (SAT/LOD/MGN/MSH/SHT/DDS)",
  "version": "1.0.0",
  "publisher": "swgemu",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onCommand:mobileDuplicator.open"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mobileDuplicator.open",
        "title": "Mobile Duplicator",
        "category": "SWG"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "@swgemu/core": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.60.0",
    "typescript": "^5.0.0"
  }
}
